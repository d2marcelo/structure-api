import StructureRootController from '../root'

class StructureOrganizationController extends StructureRootController {

  constructor(options = {}) {
    super(options)
  }

}

export default StructureOrganizationController
