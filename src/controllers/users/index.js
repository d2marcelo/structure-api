import StructureRootController from '../root'

class StructureUserController extends StructureRootController {

  constructor(options = {}) {
    super(options)
  }

}

export default StructureUserController
